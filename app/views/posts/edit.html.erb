<div id="page_wrapper">
  <h1>Edit</h1>
  <% if @post.image.file? %>
      <p>Image Currently Uploaded</p>
      <%= image_tag @post.image.url(:medium) %>
      <%= render 'form' %>
  <% else %>
      <%= render 'form' %>
  <% end %>
</div>